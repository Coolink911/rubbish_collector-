[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    # starlette 1.6's TestClient imports a deprecated anyio alias - their
    # code, not ours. Everything else stays loud.
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
